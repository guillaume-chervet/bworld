using System;
using System.Threading.Tasks;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Demo.Geo.Tests
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public async Task TestMethod1()
        {

          /*  var address1 = await new GeocodingService().ReverseAsync("1 rue de flandre, 59110, la madeleine");
            var address2 = await new GeocodingService().ReverseAsync("5 rue de lorraine, 22440, Ploufragan");
            var geoloc = await new GeocodingService().ReverseFromIpAsync("81.220.112.93");
*/
        }
    }
}
